<meta name="viewport" content="width=device-width, initial-scale=1, maximum-scale=1, user-scalable=0" />
<body>
</body>

<%
  user_id = 1
  user_hash = OpenSSL::HMAC.hexdigest(
    'sha256',
    @web_widget.hmac_token,
    user_id.to_s
  )

%>
<script>

window.tuntasSettings = {
  hideMessageBubble: false,
  // showUnreadMessagesDialog: false,
  // baseDomain: '.loca.lt',
  position: '<%= @widget_position %>',
  locale: 'en',
  useBrowserLanguage: true,
  type: '<%= @widget_type %>',
  // showPopoutButton: true,
  widgetStyle: '<%= @widget_style %>',
  darkMode: '<%= @dark_mode %>',
};

(function(d,t) {
  var BASE_URL = '';
  var g=d.createElement(t),s=d.getElementsByTagName(t)[0];
  g.src= BASE_URL + "/packs/js/sdk.js";
  g.async = true;
  s.parentNode.insertBefore(g,s);
  g.onload=function(){
    window.tuntasSDK.run({
      websiteToken: '<%= @web_widget.website_token %>',
      baseUrl: BASE_URL
    })
  }
})(document,"script");

window.addEventListener('tuntas:ready', function() {
  console.log('tuntas:ready', window.$tuntas);
  if (window.location.search.includes('setUser')) {
    window.$tuntas.setUser('<%= user_id %>', {
      identifier_hash: '<%= user_hash %>',
      email: 'jane@example.com',
      name: 'Jane Doe',
      phone_number: ''
    });
  }
})

window.addEventListener('tuntas:error', function(e) {
  console.log('tuntas:error', e.detail)
})


window.addEventListener('tuntas:on-message', function(e) {
  console.log('tuntas:on-message', e.detail)
})

window.addEventListener('tuntas:postback', function(e) {
  console.log('tuntas:postback', e.detail)
})

window.addEventListener('tuntas:opened', function() {
  console.log('tuntas:opened')
})

window.addEventListener('tuntas:closed', function() {
  console.log('tuntas:closed')
})

window.addEventListener('tuntas:on-start-conversation', function(e) {
  console.log('tuntas:on-start-conversation', e.detail)
})
</script>
